<% provide(:title, @hardware.name) %>
<div class="row">
	
		<section>
			<h1>
				<%= @hardware.name %>
			</h1>
			<% @reserves.each do |r| %>
   			<li>
      	<%= r.name %> <%= r.from_time.to_date %> <%= r.to_time.to_date %> <%= r.user.firstname  %>
    	</li>
  		<% end %>
		</section>
	



	
</div>